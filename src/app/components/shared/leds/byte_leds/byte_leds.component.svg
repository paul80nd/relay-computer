<!-- Origin is at centre of bit 7 led -->
@for (i of [7,6,5,4,3,2,1,0]; track i) {
  <svg:g>
    <circle [attr.cx]="72-(10.35*i)" cy="0" r="4" class="ledRed" [class.on]="value().bit(i)"></circle>
    </svg:g>
  }
